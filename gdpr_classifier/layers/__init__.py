"""Detection layers for the GDPR classifier.

Groups the individual layers used by the pipeline: the pattern layer
(regex and checksum based recognizers), the entity layer (NER based,
stubbed in iteration 1), and the context layer (contextual analysis,
stubbed in iteration 1).
"""
