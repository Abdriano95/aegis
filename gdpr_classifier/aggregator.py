"""Finding aggregator.

Defines the Aggregator, which merges findings produced by multiple
layers: it detects overlapping spans, resolves duplicates, and
combines evidence into a consistent set of findings used to derive
the final Classification.
"""
